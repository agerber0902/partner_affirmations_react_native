import { StyleSheet } from "react-native";

export const homeStyles = () =>
  StyleSheet.create({
    homeContainer: {
        display: "flex",
        width: "100%",

        margin: "auto",

    },
});