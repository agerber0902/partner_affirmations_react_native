import { StyleSheet } from "react-native";
import { Theme } from "../theme";

export const homeStyles = () =>
  StyleSheet.create({
    homeContainer: {
      display: "flex",
      width: "90%",

      marginLeft: "auto",
      marginRight: "auto",
    },
  });
