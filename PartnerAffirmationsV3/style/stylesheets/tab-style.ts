import { Theme } from "../theme";

export const tabStyle = {
  screenOptions: {
    borderRadius: Theme.borders.tabRadius,
    tabActiveColor: Theme.colorScheme.tabActive,
    tabInactiveColor: Theme.colorScheme.tabInactive,
    paddingVertical: 8,
    selectedIconSize: 5,
  },
};
